<!DOCTYPE html>
<html>
  <head>
    <title>The Jetsetter Wallet</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

  </head>

  <ul class="topnav">
    <li class="logo_header"><%= link_to 'The Jetsetter Wallet', root_path %></li>

    <%if !logged_in?%>
      <li><%= link_to 'Trips', trips_path %></li>
      <li class="right"><%= link_to 'Sign Up', sign_up_path %></li>
      <li class="right"><%= link_to 'Log In', log_in_path %></li>
    <%else%>
      <li><%= link_to 'All Trips', trips_path %></li>
      
        
      <li class="right"><%= link_to('Log Out', log_out_path, method: :delete, action: :destroy)%></li>
      <li class="right"><%= link_to current_user.username, user_trips_path(current_user) %></li>
      
    <%end%>

    <li><%= link_to 'Jetsetters Worldwide', users_path %></li>
  </ul>


  <body>
    <% if flash[:notice] %>
      <div class="notice"><%= flash[:notice] %></div>
    <% end %>

    <div class="page_content_container">
      <%= yield %>
    </div>
  </body>
</html>
